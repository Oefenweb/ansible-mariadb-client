# tasks file
---
- include: install.yml
  tags:
    - configuration
    - mariadb-client
    - mariadb-client-install

- include: configure.yml
  tags:
    - configuration
    - mariadb-client
    - mariadb-client-configure
